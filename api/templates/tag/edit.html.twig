{% extends 'layout.html.twig' %}

{% block title %}Edit tag (ID: {{ tag.id }}){% endblock %}

{% block stylesheets %}
    <link href="{{ asset('css/submit.css') }}" rel="stylesheet">
{% endblock %}

{% block main %}
    <h1 class="text-center" style="margin-bottom: 30px">Edit tag (ID: {{ tag.id }})</h1>
    <div class="container" style="max-width: 50%">
        {{ form(tag_form, {'attr': {'novalidate': 'novalidate'}}) }}
    </div>
{% endblock %}
